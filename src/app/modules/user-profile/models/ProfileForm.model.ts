export class profilForm {

  constructor(
    public id ? : number,
    public title ? : string,
    public description ? : string,
    public images ? : string,
    public online ? : boolean,
  ) {}

}
